import { IconValue } from "@home-assistant-react/ui/src";

export interface ActionOptions {
  entity_id: string;
  custom_label: string;
  custom_icon: IconValue;
}

export interface ActionPanelState {}
